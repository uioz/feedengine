export * from './types/index.js';
import {definePlugin} from 'feedengine-plugin';

export const plugin = definePlugin(() => {
  // context.getSequelize().define<AtomPluginMainTableModel>('test',{

  // })

  return {};
});
